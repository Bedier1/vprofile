apiVersion: v1
kind: Service
metadata:
  name: {{ include "vprofilechart.fullname" . }}
  labels:
    {{- include "vprofilechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vprofilechart.selectorLabels" . | nindent 4 }}
